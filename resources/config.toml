[connection]
port = 8080
certificate_path = "../resources/secret/cert.pem"
private_key_path = "../resources/secret/key.pem"

[served_content]
static_files_source_dir = "../resources/static"
http_404_error_page = "../resources/static/custom-404-page.html"

[performance]
# Set to 0 or omit completely to use hw threads count returned by std::thread::hardware_concurrency() 
max_thread_count = 0
